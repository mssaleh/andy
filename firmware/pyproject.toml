[project]
name = "andy-firmware-tools"
version = "0.1.0"
description = "Project-local ESPHome toolchain for Andy"
requires-python = ">=3.12,<3.13"
dependencies = [
    "esphome==2026.8.0",
]

[tool.uv]
package = false
